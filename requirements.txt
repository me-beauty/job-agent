# Job Agent — Pragmatic Dependencies
# pip install -r requirements.txt

# Core
flask>=3.0.0
markdown>=3.5.0
python-dotenv>=1.0.0
requests>=2.31.0

# Search Engine (Baidu / DuckDuckGo)
duckduckgo-search>=8.0.0

# Vector Store (numpy only, no chromadb)
numpy>=1.24.0

# AI/ML (optional, fallback to keyword rules if not installed)
torch>=2.0.0

# Testing
pytest>=8.0.0
